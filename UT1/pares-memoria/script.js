/*
(int) (casillas / 2)

set aleatorios

while (aleatorios.size < (int) (casillas / 2 ) ) {
    int random = (int) (Math.random() * casillas);
    if (!aleatorios.contains(random)) {
        aleatorios.add(random);
    }
}

while array.size < cuantia {
    rnd = aleatoriogenerado()
}

*/